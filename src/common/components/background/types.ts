import { Sx } from '../../types';

export type CircleProps = {
  color: string;
  size: string | number;
  left?: string | number;
  right?: string | number;
  top?: string | number;
  bottom?: string | number;
  sx?: Sx;
};
